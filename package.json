{
  "name": "workers-on-edge-runtime",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle src/server.ts --outfile=dist/server.js",
    "dev": "edge-runtime dist/server.js --listen",
    "test": "jest"
  },
  "license": "MIT",
  "dependencies": {
    "hono": "^1.5.2"
  },
  "devDependencies": {
    "@edge-runtime/jest-environment": "^1.1.0-beta.10",
    "@edge-runtime/types": "^1.1.0-beta.10",
    "@types/jest": "^28.1.3",
    "edge-runtime": "^1.1.0-beta.10",
    "esbuild": "^0.14.47",
    "esbuild-jest": "^0.5.0",
    "jest": "^28.1.1"
  }
}